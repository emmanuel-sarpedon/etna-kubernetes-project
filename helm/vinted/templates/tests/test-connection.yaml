apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vinted.fullname" . }}-test-connection"
  labels:
{{ include "vinted.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vinted.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
